#ifndef CALIBRATE_H
#define CALIBRATE_H

#include <QObject>

class Calibrate : public QObject
{
    Q_OBJECT
public:
    explicit Calibrate(QObject *parent = nullptr);

signals:

public slots:
};

#endif // CALIBRATE_H
